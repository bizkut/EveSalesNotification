# ------------------------------------------------------------------
# Environment variables for Docker Compose
#
# Create a .env file by copying this file and replacing the values.
# ------------------------------------------------------------------

# Your Cloudflare Tunnel token.
# This is required for the `cloudflared` service to connect to Cloudflare
# and expose the webapp to the internet for the EVE Online SSO callback.
#
# 1. Create a tunnel: `cloudflared tunnel create your-tunnel-name`
# 2. Get the token: `cloudflared tunnel token your-tunnel-name`
TUNNEL_TOKEN="your_cloudflare_tunnel_token_here"